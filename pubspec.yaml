name: flutter_component
description: Ready-made component base using ready-made abstractions for quick use and decomposed by platform.
version: 1.0.0
repository: https://github.com/ivangalkindeveloper/flutter_component
# homepage: https://www.example.com
publish_to: none

environment:
  sdk: ">=2.18.4 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  animate_do:
    path: package/animate_do
  animations:
    path: package/animations
  badges:
    path: package/badges
  custom_rounded_rectangle_border:
    path: package/custom_rounded_rectangle_border
  flutter_vibrate:
    path: package/flutter_vibrate
  modal_bottom_sheet:
    path: package/modal_bottom_sheet
  pinput:
    path: package/pinput
    
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true